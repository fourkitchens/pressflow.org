<?php

function get_pressflow_version($ver = 6) {
  $file_url = "https://raw.github.com/pressflow/" . $ver . "/master/CHANGELOG.  txt";
  $file = file_get_contents($file_url, NULL, NULL, 7, 15);
  $array = explode(',', $file);
  return $array[0];
}